<?xml version="1.0"?>
<iq:manifest version="3" xmlns:iq="http://www.garmin.com/xml/connectiq">
    <!--
      WingFoil — INVITE BETA channel (docs/decisions.md ADR-012). Identical code to
      manifest.xml; separate UUID so it is its own store listing. This one is meant to be
      PUBLIC because CIQ beta listings are developer-only, and it is locked on first launch
      until the tester enters the unlock key minted by lab/tools/make_unlock.py. Build it
      only through monkey-invite.jungle, which supplies the pepper that arms the lock.
    -->
    <iq:application id="28942317-5a50-4fed-8a9e-d62f6847a2db" type="watch-app"
        name="@Strings.AppNameInvite" entry="WingfoilApp" launcherIcon="@Drawables.LauncherIcon"
        minApiLevel="5.0.0">
        <iq:products>
            <iq:product id="fenix843mm"/>
            <iq:product id="fenix847mm"/>
            <iq:product id="fenix8solar47mm"/>
            <iq:product id="fenix8solar51mm"/>
            <iq:product id="fenix8pro47mm"/>
        </iq:products>
        <iq:permissions>
            <iq:uses-permission id="Fit"/>
            <iq:uses-permission id="FitContributor"/>
            <iq:uses-permission id="Positioning"/>
            <iq:uses-permission id="Sensor"/>
            <iq:uses-permission id="SensorLogging"/>
        </iq:permissions>
        <iq:languages>
            <iq:language>eng</iq:language>
            <iq:language>deu</iq:language>
        </iq:languages>
        <iq:barrels>
            <iq:depends name="WingFoilCore" version="1.0.0"/>
        </iq:barrels>
    </iq:application>
</iq:manifest>
